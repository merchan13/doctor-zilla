<%= render 'shared/errors', obj: @record %>
<div class="col-md-9 new-panel main-panel">

  <h3 align="center"><strong>Nueva Informe Médico</strong></h3>

  <div class="col-md-12"><hr></div>

  <%= form_for(@record, :html => {class: 'form-horizontal', role: 'form'}) do |f| %>

    <div class="form-group col-md-12">
      <div class="col-md-12">
        <%= text_area_tag(:bg_family, @data, rows: '30', class: 'form-control') %>
      </div>
    </div>

    <div class='form-group center'>
      <div class='col-md-offset-4 col-md-4'>
        <%= f.submit class: 'btn btn-primary btn-lg' %>
      </div>
    </div>
  <% end %>
</div>

<%= render partial: 'medical_records/side_menu', locals: { record: @record } %>
